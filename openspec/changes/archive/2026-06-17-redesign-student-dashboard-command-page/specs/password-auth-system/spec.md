## REMOVED Requirements

### Requirement: First-login password change
**Reason**: The redesigned dashboard removes the old dashboard-level temporary-password prompt after auth cleanup and password reset flows have superseded it.
**Migration**: Students use the existing password reset/account recovery flow when they need to change credentials. The dashboard no longer blocks or highlights the command page with the legacy password-change prompt.
