## 1. New Layout

- [x] 1.1 Create `src/app/(public)/instructor-registration/layout.tsx` mirroring `onboarding/layout.tsx` with compact branding column and wide content card
- [x] 1.2 Verify the layout renders correctly at desktop and mobile viewports

## 2. Verification

- [x] 2.1 Run `npm run build` to confirm no compilation errors
- [x] 2.2 Confirm other `(public)/` pages (login, etc.) are unaffected
